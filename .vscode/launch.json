{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Mac Run",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "preLaunchTask": "cmake-build"
        }
    ]
}
